TODOs:
	- fix HPBar (brackey's)
	- force player to walk within map bounds
	- Should state already statused if another status move is used, even if it misses
		- When this condition is met, ensure that the pokemon named is the right one
	- Fix all dialogue's to be skippable with space bar
	- Implement Struggle if pokemon has 0 PP in all moves
	- Fix Player and Enemy Battle HUD graphics
	- Implement a volatile status counter for each volatile status (e.g. you can be confused and perish song'd at the same time)
	- Add dialogue to allow player to say no to learning a new move instead of selecting to forget the new move
	- Fix tiling issues with sizes of tile vs camera view in different scenes
	- Fix Party Screen height
	- Don't allow mouse to scroll within Menu UIs
	- Fix divide by zero in SetHPBar function
	- Fix window height (currently blue around the top and bottom borders) during battle
	- Fix text in party screen and battle screen to display 'HP' instead of just 'H' for each pokemon
	- Display text for Items in Dialogue within InventoryUI even if its null in UseItem() (currently displays 'It' instead of ItemName)
	- Make Bag Save/Load its contents to file when Save/Load is called
	- Make Ethers recover PP for only one move instead of all (ensure elixirs only work if at least one move has lost PP)
	- Sort TMs alphanumerically (i.e. TM01 should always be before TM02 in bag order)
	- Get Generation 3 TM bag sprites (get all type colours; get spritesheet of all TM sprites if possible)
	- Add Looping Around the MoveSelectionUI List when learning a new move while already having 4 moves
	- Remove WaitForSeconds after learning replacing a move (i.e. you already had 4 and then learned another) and fix the fact that the DialogueManager is still trying to print the move
	- Hide count of item in TMs in InventoryUI
	- Rate Limit user button input to stop keyboard spam